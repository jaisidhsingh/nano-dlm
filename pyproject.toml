[project]
name = "nano-dlm"
version = "0.1.0"
description = "A nanoGPT equivalent for diffusion LMs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flax>=0.12.5",
    "jax>=0.9.1",
    "jaxlib>=0.9.1",
    "tyro>=1.0.8",
]
